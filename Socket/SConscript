Import('env')
import SatSCons

###########################################################################

sources = SatSCons.GlobSources()

SatSCons.StandardTargets(env)

SatSCons.Lib(env,
             library = 'Socket',
             sources = sources,
             LIBS = [ 'Utils' ])

SatSCons.Doxygen(env,sources)
